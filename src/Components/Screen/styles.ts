import {StyleSheet} from 'react-native';

import {BACKGROUND_APP_COLOR} from '../../Common/Constants/colors';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingVertical: 25,
    paddingHorizontal: 15,
    backgroundColor: BACKGROUND_APP_COLOR,
  },
});
